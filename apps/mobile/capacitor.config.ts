import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "dev.blacksail.piratebattle",
  appName: "Pirate-Battle",
  webDir: "../web/dist",
};

export default config;

// Stub ESLint flat config (ESLint 9+).
// TASK-006 expands this with TS parser, recommended rules, react plugin
// scoped to apps/web, and import-order rule. This stub keeps `eslint .`
// from failing during the bootstrap window between TASK-001 and TASK-006.
export default [{}];

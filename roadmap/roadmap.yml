version: 1
meta:
  project: Pirate-Battle
  created: '2026-05-08'
  branch_prefix: auto/
  task_id_format: TASK-{seq:03d}
epics:
  - id: EPIC-01
    title: Foundation — monorepo, server, battle engine v1
    description: |
      Establish the technical spine: TypeScript monorepo (apps/{web,server,
      discord,mobile} + packages/{core,content,shared,db}), Postgres-backed
      Fastify server, and a server-authoritative turn-based 6v6 swap-in
      battle engine. Ships with a free starter crew set so the game is
      end-to-end playable before any Cardano integration. The engine lives
      in packages/core so web, server, and Discord all share one rules
      implementation.
    stories:
      - id: STORY-01
        title: Monorepo scaffold + green CI
        description: >-
          npm workspaces + TS project refs + skeleton apps + first green CI run that exercises real
          Node steps.
        tasks:
          - id: TASK-001
            title: Init root package.json + npm workspaces + tsconfig.base.json
            status: done
            priority: high
            complexity: small
            workspaces:
              - root
            description: |
              Create root package.json (private, name "pirate-battle",
              workspaces: ["apps/*", "packages/*"]), tsconfig.base.json
              with strict mode + composite refs settings, and a root
              tsconfig.json that references all workspaces.
              Add scripts: typecheck (tsc -b), test, lint, build, dev.
              Run npm install at root to generate package-lock.json.
            depends_on: []
            pr: https://github.com/g-chappell/pirate-battle/pull/4
            completed: '2026-05-08T11:24:00Z'
            blocked_reason: null
            last_attempted: '2026-05-08T11:13:00Z'
            attempt_count: 1
          - id: TASK-002
            title: Scaffold packages/{core,content,shared,db} with composite tsconfigs
            status: done
            priority: high
            complexity: small
            workspaces:
              - packages/core
              - packages/content
              - packages/shared
              - packages/db
            description: |
              For each of packages/core, packages/content, packages/shared,
              packages/db: add package.json (name "@pirate-battle/<pkg>",
              version 0.0.0, type "module"), tsconfig.json with composite:
              true extending tsconfig.base, src/index.ts placeholder export.
              Add cross-references in tsconfig.json `references` arrays
              respecting layering: core depends on nothing; content depends
              on core; shared depends on core; db is leaf.
            depends_on:
              - TASK-001
            pr: https://github.com/g-chappell/pirate-battle/pull/4
            completed: '2026-05-08T11:24:00Z'
            blocked_reason: null
            last_attempted: null
            attempt_count: 0
          - id: TASK-003
            title: Scaffold apps/server (Fastify) with /health endpoint
            status: done
            priority: high
            complexity: small
            workspaces:
              - apps/server
            description: |
              apps/server with package.json (deps: fastify, dev: tsx, vitest,
              @types/node), tsconfig.json (composite, references core +
              shared + db), src/index.ts with a Fastify instance returning
              { ok: true } on /health. dev script via tsx watch. start
              script via node dist/index.js. Build via tsc -b.
            depends_on:
              - TASK-002
            pr: https://github.com/g-chappell/pirate-battle/pull/6
            completed: '2026-05-08T12:09:00Z'
            blocked_reason: null
            last_attempted: '2026-05-08T12:04:00Z'
            attempt_count: 1
          - id: TASK-004
            title: Scaffold apps/web (Vite + React) with placeholder route
            status: done
            priority: high
            complexity: small
            workspaces:
              - apps/web
            description: |
              apps/web with package.json (deps: react, react-dom; devDeps:
              vite, @vitejs/plugin-react, @types/react, vitest), vite.config.ts,
              tsconfig.json (composite, references core + shared), index.html,
              src/main.tsx mounting <App/>, src/App.tsx returning the project
              name + a "Connect wallet" placeholder. dev on port 5173.
              Build outputs to dist/.
            depends_on:
              - TASK-002
            pr: https://github.com/g-chappell/pirate-battle/pull/7
            completed: '2026-05-08T13:04:00Z'
            blocked_reason: null
            last_attempted: '2026-05-08T13:01:00Z'
            attempt_count: 1
          - id: TASK-005
            title: Scaffold apps/discord stub + apps/mobile placeholder
            status: done
            priority: med
            complexity: small
            workspaces:
              - apps/discord
              - apps/mobile
            description: |
              apps/discord: package.json with discord.js v14+ dependency,
              tsconfig.json (composite, references core + shared), src/index.ts
              with a no-op placeholder (export {}). Real bot lands in EPIC-05.
              apps/mobile: just a README explaining Capacitor wrap arrives in
              EPIC-06; do NOT install Capacitor yet.
            depends_on:
              - TASK-002
            pr: https://github.com/g-chappell/pirate-battle/pull/42
            completed: '2026-05-10T15:05:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T15:03:00Z'
            attempt_count: 2
          - id: TASK-006
            title: ESLint + Prettier root config (flat config)
            status: done
            priority: med
            complexity: small
            workspaces:
              - root
            description: |
              eslint.config.js (flat config, ESLint 9+) at root with TS
              parser, recommended rules, react plugin scoped to apps/web,
              import-order rule. .prettierrc at root. Add lint + format
              scripts to root package.json. Verify `npx eslint .` and
              `npx prettier --check .` both pass on the scaffolded code.
            depends_on:
              - TASK-005
            pr: https://github.com/g-chappell/pirate-battle/pull/43
            completed: '2026-05-10T16:13:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T16:04:00Z'
            attempt_count: 1
      - id: STORY-02
        title: Postgres schema + Prisma migrations
        description: >-
          packages/db with Prisma schema covering users, captains, crews, moves, items, battles,
          battle_events. Dev seed + migration workflow.
        tasks:
          - id: TASK-007
            title: 'Prisma schema: users, captains, crews, moves, items'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - packages/db
            description: |
              packages/db/prisma/schema.prisma with models:
              - User (id, stakeAddr unique nullable, createdAt)
              - Captain (id, userId FK, name, factionId, createdAt)
              - Crew (id, captainId FK, templateKey, level, xp, attrs JSON,
                createdAt)
              - CrewMove (id, crewId FK, moveKey, slot, isLearned)
              - Item (id, ownerUserId FK, templateKey, qty)
              Generator output to packages/db/generated/client. Add to
              .gitignore. Add prisma/seed.ts placeholder. db
              connection from DATABASE_URL env.
            depends_on:
              - TASK-002
            pr: https://github.com/g-chappell/pirate-battle/pull/8
            completed: '2026-05-08T14:06:00Z'
            blocked_reason: null
            last_attempted: '2026-05-08T14:03:00Z'
            attempt_count: 1
          - id: TASK-008
            title: 'Prisma schema: battles + battle_events + initial migration'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - packages/db
            description: |
              Add to schema.prisma:
              - Battle (id, mode (PvE/PvP/AI), participantA/B userIds,
                resultJson JSON, seed Bytes, startedAt, endedAt nullable)
              - BattleEvent (id, battleId FK, idx int, kindStr, payloadJson)
              - Index battleId+idx for replay scan
              Run `prisma migrate dev --name init` to create the first
              migration. Commit prisma/migrations/.
              Verify generated client compiles + exports the models.
              Export a configured PrismaClient from packages/db/src/index.ts.
            depends_on:
              - TASK-007
            pr: https://github.com/g-chappell/pirate-battle/pull/9
            completed: '2026-05-08T15:06:00Z'
            blocked_reason: null
            last_attempted: '2026-05-08T15:01:00Z'
            attempt_count: 1
      - id: STORY-03
        title: Battle engine v1 (deterministic, replay-able)
        description: >-
          Pure-TS engine in packages/core: turn loop, action queue, swap-in, faint, 4-affinity type
          matrix, basic moves. Same seed → same outcome.
        tasks:
          - id: TASK-009
            title: Engine types + state shape (BattleState, Crew, Move, Action)
            status: done
            priority: high
            complexity: medium
            workspaces:
              - packages/core
            description: |
              packages/core/src/types.ts: TS types for BattleState (turn,
              activeA, activeB, benchA, benchB, log, rngSeed, rngState),
              CrewSnapshot (hp, maxHp, atk, def, spd, affinity, statuses,
              moves), MoveDef (key, name, affinity, basePower, accuracy,
              kind: damage/status/buff, statusEffect?), Action union (move,
              switch, forfeit). Affinity union: 'kraken'|'ironclad'|'phantom'|
              'bloodborne'. No I/O. Export from src/index.ts.
            depends_on:
              - TASK-002
            pr: https://github.com/g-chappell/pirate-battle/pull/11
            completed: '2026-05-08T17:09:00Z'
            blocked_reason: null
            last_attempted: '2026-05-08T17:06:00Z'
            attempt_count: 1
          - id: TASK-010
            title: 'Engine: turn loop + action queue + swap-in + faint'
            status: done
            priority: high
            complexity: large
            workspaces:
              - packages/core
            description: |
              packages/core/src/engine.ts: pure functions resolveTurn(state,
              actionA, actionB, rng) → newState. Order: switch actions
              resolve first (both at once); then move actions resolve in
              priority then speed order; faint check; auto-prompt for swap
              if active fainted; battle ends when one side has no
              non-fainted crew. Seeded RNG via simple xoshiro128 or mulberry32
              (deterministic, no Math.random). Append BattleEvent objects to
              state.log. No mutation — return new state each call.
            depends_on:
              - TASK-009
            pr: https://github.com/g-chappell/pirate-battle/pull/12
            completed: '2026-05-08T18:24:00Z'
            blocked_reason: null
            last_attempted: '2026-05-08T18:02:00Z'
            attempt_count: 1
          - id: TASK-011
            title: 'Engine: damage formula, accuracy, status effects, type matrix'
            status: done
            priority: high
            complexity: large
            workspaces:
              - packages/core
            description: |
              In packages/core/src/engine.ts (or split into
              src/resolveMove.ts): implement damage = floor(((2*lvl/5+2) *
              power * atk/def) / 50) + 2 with affinity multiplier from a
              4×4 type chart. Accuracy roll uses RNG. Status effects:
              poison (1/8 max hp end of turn), burn (1/16 + atk halved),
              stun (skip turn, 30% off-chance). Crit on 1/16 default.
              All values exported as constants for tuning.
              Type matrix (attacker → defender): kraken strong vs ironclad
              and bloodborne; ironclad strong vs phantom; phantom strong vs
              kraken; bloodborne strong vs phantom.
            depends_on:
              - TASK-010
            pr: https://github.com/g-chappell/pirate-battle/pull/14
            completed: '2026-05-09T11:20:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T11:11:00Z'
            attempt_count: 1
          - id: TASK-012
            title: Engine determinism unit tests (Vitest)
            status: done
            priority: high
            complexity: medium
            workspaces:
              - packages/core
            description: |
              packages/core/src/engine.test.ts: given fixed initial state +
              fixed action sequence + fixed seed, the resulting log is
              byte-equal across 100 runs. Cover: simple damage exchange,
              status proc on right turn, faint + swap-in, side-victory.
              These tests are the highest-leverage tests in the codebase —
              break them and you've broken replays. Configure Vitest at
              workspace level.
            depends_on:
              - TASK-011
            pr: https://github.com/g-chappell/pirate-battle/pull/15
            completed: '2026-05-09T12:25:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T12:20:00Z'
            attempt_count: 1
      - id: STORY-04
        title: Free starter crew + content package
        description: >-
          8 hand-authored crews (2 per OTK faction) + 16 base moves (4 per affinity), plus item
          templates. All in packages/content with strict types.
        tasks:
          - id: TASK-013
            title: Author 16 base moves (4 per affinity) in packages/content
            status: done
            priority: high
            complexity: medium
            workspaces:
              - packages/content
            description: |
              packages/content/src/moves.ts exporting an array typed
              against MoveDef from packages/core. 4 moves per affinity:
              kraken (e.g. Tide Surge, Ink Cloud, Tentacle Lash, Maelstrom),
              ironclad (e.g. Cannonade, Hull Plate, Rivet Salvo, Iron Will),
              phantom (e.g. Vanish, Phantom Strike, Mirage, Blood Fade),
              bloodborne (e.g. Cutlass Combo, Boarding Charge, Berserk, Last
              Stand). Mix damage / status / buff per affinity. Cite OTK
              lore where applicable; mark [DRAFT] for unconfirmed flavour.
            depends_on:
              - TASK-009
            pr: https://github.com/g-chappell/pirate-battle/pull/16
            completed: '2026-05-09T13:09:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T13:06:00Z'
            attempt_count: 1
          - id: TASK-014
            title: Author 8 starter crews (2 per faction) with stats + 4 moves each
            status: done
            priority: high
            complexity: medium
            workspaces:
              - packages/content
            description: |
              packages/content/src/crews.ts: 8 starter crew templates,
              keyed by templateKey, with affinity, base attrs (hp, atk,
              def, spd) totalling ~250, 4 starter move keys, lore blurb.
              2 per faction. Include: 1 balanced + 1 specialist per
              faction. Rare/legendary tiers come later — these are common
              starters anyone can play with.
            depends_on:
              - TASK-013
            pr: https://github.com/g-chappell/pirate-battle/pull/18
            completed: '2026-05-09T14:07:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T14:01:00Z'
            attempt_count: 1
  - id: EPIC-02
    title: Web client v1 — first playable battle
    description: |
      First playable web client. Player connects (anonymous starter session
      — no wallet yet), assembles a team from the free starter crew, fights
      one PvE opponent, sees turn-by-turn animated battle. Validates engine
      + UX shape before tackling identity (EPIC-03).
    stories:
      - id: STORY-05
        title: Anonymous starter session + team builder UI
        tasks:
          - id: TASK-015
            title: 'Server: anonymous session create + cookie + GET /me'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/server
              - packages/db
            description: |
              apps/server: POST /api/session/anonymous creates a User row
              with stakeAddr=null, sets a signed session cookie (use
              @fastify/cookie + a SESSION_SECRET-keyed signature). GET /me
              returns the current user (id, stakeAddr, captains[]). 401 if
              no cookie. Server-side session store can be JWT-style for now.
            depends_on:
              - TASK-008
              - TASK-003
            pr: https://github.com/g-chappell/pirate-battle/pull/19
            completed: '2026-05-09T15:11:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T15:03:00Z'
            attempt_count: 1
          - id: TASK-016
            title: 'Web: starter session bootstrap + team builder UI'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/web
            description: |
              apps/web: on app load, if no session cookie, POST
              /api/session/anonymous. Display the free starter crew
              (read from packages/content) and let user pick 6 for their
              team. POST /api/captain to persist (server creates Captain +
              6 Crew rows). UI in plain React for now — Phaser comes in
              STORY-06. Use TanStack Query or a small fetch wrapper.
            depends_on:
              - TASK-015
              - TASK-014
              - TASK-004
            pr: https://github.com/g-chappell/pirate-battle/pull/20
            completed: '2026-05-09T16:14:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T16:05:00Z'
            attempt_count: 1
      - id: STORY-06
        title: Phaser battle scene
        tasks:
          - id: TASK-017
            title: 'Web: install Phaser 3 + scaffold BootScene/BattleScene'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/web
            description: |
              Install phaser in apps/web. Add a Phaser game instance bound
              to a <canvas> rendered inside a React component. BootScene
              loads placeholder sprites (4 affinity-coloured rectangles
              for now). BattleScene takes a BattleState prop and displays
              the active crew + HP bars. Don't build the move menu yet.
            depends_on:
              - TASK-016
            pr: https://github.com/g-chappell/pirate-battle/pull/21
            completed: '2026-05-09T17:11:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T17:04:00Z'
            attempt_count: 1
          - id: TASK-018
            title: 'Web: BattleScene HP bars + move menu + swap UI'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/web
            description: |
              Extend BattleScene: visible HP bars (Phaser Graphics rects),
              move menu (DOM overlay React component, NOT in canvas — per
              CLAUDE.md "React owns DOM chrome, Phaser owns canvas"), swap
              button revealing bench. Wire submit → POST /api/battle/<id>/action.
              Display turn log in a side panel (also React DOM).
            depends_on:
              - TASK-017
            pr: https://github.com/g-chappell/pirate-battle/pull/22
            completed: '2026-05-09T18:09:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T18:03:00Z'
            attempt_count: 1
          - id: TASK-019
            title: 'Web: turn animation placeholders (sprite shake + flash)'
            status: done
            priority: med
            complexity: small
            workspaces:
              - apps/web
            description: |
              Minimal hit feedback: when an attack lands, flash the target
              red and add a brief x-offset shake via Phaser tweens. When
              a crew faints, fade the sprite. Real sprite art arrives
              post-MVP — these placeholders verify the animation hooks
              are wired.
            depends_on:
              - TASK-018
            pr: https://github.com/g-chappell/pirate-battle/pull/47
            completed: '2026-05-10T20:11:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T20:08:00Z'
            attempt_count: 2
      - id: STORY-07
        title: First PvE opponent + win/loss screens
        tasks:
          - id: TASK-020
            title: 'Server: AI opponent + battle create/resolve endpoints'
            status: done
            priority: high
            complexity: large
            workspaces:
              - apps/server
              - packages/core
            description: |
              apps/server: POST /api/battle/start — given userId, create a
              Battle row, generate an AI opponent (fixed lore-flavoured team
              for v1), seed RNG. POST /api/battle/:id/action — accept a
              user Action, run AI's action via simple heuristic (pick the
              move with highest expected damage given affinity matrix),
              call resolveTurn from packages/core, persist BattleEvents,
              return new state. GET /api/battle/:id — current state.
            depends_on:
              - TASK-012
              - TASK-016
            pr: https://github.com/g-chappell/pirate-battle/pull/24
            completed: '2026-05-09T19:18:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T19:07:00Z'
            attempt_count: 1
          - id: TASK-021
            title: 'Web: win/loss screens + battle history list'
            status: done
            priority: med
            complexity: small
            workspaces:
              - apps/web
            description: |
              On Battle.endedAt set, BattleScene transitions to a result
              modal: winner, turn count, summary stats per crew. Button
              "Battle again" creates a new Battle. History list page
              shows the user's last 10 Battle rows with mode + result.
            depends_on:
              - TASK-020
              - TASK-019
            pr: https://github.com/g-chappell/pirate-battle/pull/48
            completed: '2026-05-10T21:27:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T21:11:00Z'
            attempt_count: 1
  - id: EPIC-03
    title: Cardano wallet identity & NFT roster
    description: |
      Replace anonymous sessions with wallet-based identity. CIP-30 on web,
      signed-message auth, NFT discovery from an admin-managed allow-list of
      Cardano collections via Blockfrost, deterministic trait → stat mapping.
      Free starter remains usable; NFT crews add to the available roster.
    stories:
      - id: STORY-08
        title: CIP-30 wallet connect + signed-message auth
        tasks:
          - id: TASK-022
            title: 'Web: CIP-30 wallet chooser + connect flow'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/web
            description: |
              apps/web: detect window.cardano keys, present a wallet
              chooser (Nami, Eternl, Lace, Typhon at minimum), call
              wallet.enable(). Get reward (stake) address. Display a
              "Connected as <truncated bech32 addr>" indicator. Persist
              chosen wallet in localStorage so re-enable on reload skips
              the prompt when wallet.isEnabled() is true.
            depends_on:
              - TASK-016
            pr: https://github.com/g-chappell/pirate-battle/pull/25
            completed: '2026-05-09T20:11:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T20:01:00Z'
            attempt_count: 1
          - id: TASK-023
            title: 'Server: signed-message auth (POST /api/auth/wallet)'
            status: done
            priority: high
            complexity: large
            workspaces:
              - apps/server
            description: |
              apps/server: POST /api/auth/nonce returns a fresh nonce +
              5-min expiry, stored server-side. POST /api/auth/wallet
              accepts { stakeAddr, payloadHex, signature, key } from CIP-30
              signData. Verify with cardano-message-signing-nodejs +
              cardano-serialization-lib-nodejs: signature math, pubkey
              hashes to claimed addr, payload contains the issued nonce
              that hasn't been used. On success: find or create User by
              stakeAddr, migrate any anonymous session's data (captains
              etc.) to the new userId, set session cookie. Reject 401 with
              clear reason on mismatch.
            depends_on:
              - TASK-022
              - TASK-015
            pr: https://github.com/g-chappell/pirate-battle/pull/26
            completed: '2026-05-09T21:18:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T21:04:00Z'
            attempt_count: 1
          - id: TASK-024
            title: 'Web: sign-in flow integration (request nonce → signData → POST)'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/web
            description: |
              Web client calls /api/auth/nonce, builds a human-readable
              login message including the nonce, calls api.signData(stakeAddr,
              payloadHex), POSTs to /api/auth/wallet. On 200: navigate to
              roster page. Display friendly errors for: user-cancelled,
              network mismatch, signature failed.
            depends_on:
              - TASK-023
            pr: https://github.com/g-chappell/pirate-battle/pull/27
            completed: '2026-05-09T22:18:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T22:04:00Z'
            attempt_count: 1
      - id: STORY-09
        title: NFT discovery service (Blockfrost) with allow-list + caching
        tasks:
          - id: TASK-025
            title: 'Server: Blockfrost client + allow-list config + NFT fetch'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/server
              - packages/db
            description: |
              apps/server/src/cardano/blockfrost.ts: typed wrapper around
              @blockfrost/blockfrost-js using BLOCKFROST_PROJECT_ID +
              BLOCKFROST_NETWORK env vars. fetchUserNfts(stakeAddr) →
              filters accountAddressesAssetsAll by allow-list policy IDs
              from NFT_ALLOWLIST_POLICY_IDS env. Cache results in a new
              NftSnapshot table (Prisma migration) keyed (userId,
              fetchedAt) with 5-min freshness window before refetch.
            depends_on:
              - TASK-008
              - TASK-023
            pr: https://github.com/g-chappell/pirate-battle/pull/28
            completed: '2026-05-09T23:13:00Z'
            blocked_reason: null
            last_attempted: '2026-05-09T23:05:00Z'
            attempt_count: 1
          - id: TASK-026
            title: 'Server: GET /api/roster (free starter + NFT crews)'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/server
            description: |
              GET /api/roster returns { starter: [...], nft: [...] } —
              starter is the 8 free crews from packages/content;
              nft is what fetchUserNfts found, mapped through the
              trait→stat pipeline (TASK-027). Web team builder consumes
              this to populate the chooser.
            depends_on:
              - TASK-025
            pr: https://github.com/g-chappell/pirate-battle/pull/30
            completed: '2026-05-10T00:08:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T00:01:00Z'
            attempt_count: 1
      - id: STORY-10
        title: Trait → stat derivation pipeline + admin tooling
        tasks:
          - id: TASK-027
            title: 'Trait→stat derivation: deterministic mapper + collection registry'
            status: done
            priority: high
            complexity: large
            workspaces:
              - apps/server
              - packages/db
              - packages/shared
            description: |
              packages/shared/src/nftMapping.ts: pure function
              deriveCrewStats(metadata, collectionRules) → CrewSnapshot.
              Rules are JSON keyed by trait name → stat delta. Add a
              Collection table (Prisma) holding policyId, name, ruleJson.
              Server: at boot, load all Collection rows; deriveCrewStats
              applied to each NFT in fetchUserNfts. Deterministic — same
              input → same output, no RNG, no DB lookup mid-derive.
            depends_on:
              - TASK-026
            pr: https://github.com/g-chappell/pirate-battle/pull/38
            completed: '2026-05-10T11:21:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T11:19:00Z'
            attempt_count: 2
          - id: TASK-028
            title: 'Admin: CLI tool to register a new collection (policyId + ruleJson)'
            status: done
            priority: med
            complexity: medium
            workspaces:
              - apps/server
              - packages/db
            description: |
              apps/server/scripts/register-collection.ts (CLI). Args:
              --policy <id>, --name <text>, --rules <jsonFile>.
              Validates JSON shape (matches collectionRules schema), inserts
              into Collection table. Re-runnable as upsert. Document in
              docs/ADMIN.md (new file). Bonus: --dry-run.
            depends_on:
              - TASK-027
            pr: https://github.com/g-chappell/pirate-battle/pull/49
            completed: '2026-05-10T22:20:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T22:06:00Z'
            attempt_count: 1
  - id: EPIC-04
    title: Crew progression, items & battle stats
    description: |
      The meta-game layer that turns one-off battles into a campaign. XP per
      battle, level-up, attribute training, items dropped + applied to crews,
      battle-history with replays. Persistence keyed on User; survives across
      sessions and devices.
    stories:
      - id: STORY-11
        title: XP, level-up, attribute training UI
        tasks:
          - id: TASK-029
            title: 'Server: XP grant on battle end + level-up curve'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/server
              - packages/core
            description: |
              On Battle.endedAt set, server grants XP to participating
              Crew rows (winner ×1.5, loser ×1.0, scaled by opponent
              level). Level curve: lvl² × 50 XP. On level up: each crew
              gains base attrs * 1.05 ratio, capped per template. Engine's
              CrewSnapshot consumes Crew.level + attrs at battle start.
            depends_on:
              - TASK-020
              - TASK-007
            pr: https://github.com/g-chappell/pirate-battle/pull/39
            completed: '2026-05-10T12:10:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T12:08:00Z'
            attempt_count: 2
          - id: TASK-030
            title: 'Web: crew detail screen + attribute training UI'
            status: done
            priority: med
            complexity: medium
            workspaces:
              - apps/web
            description: |
              Web: clicking a crew opens a detail panel — sprite, lore
              blurb, stats, moves, XP bar. "Train" button spends an in-game
              currency (TrainingChip Item) to bias one stat (+1 atk or +1
              def or +1 spd). 1 chip = 1 stat point, capped at +20% of
              base per stat. Server enforces caps server-side.
            depends_on:
              - TASK-029
            pr: https://github.com/g-chappell/pirate-battle/pull/50
            completed: '2026-05-10T23:19:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T23:04:00Z'
            attempt_count: 1
      - id: STORY-12
        title: 'Item system: drops, inventory, apply-to-crew'
        tasks:
          - id: TASK-031
            title: 'Server: item drops on battle win + inventory endpoint'
            status: done
            priority: med
            complexity: medium
            workspaces:
              - apps/server
              - packages/content
            description: |
              packages/content/src/items.ts: 8 starter item templates
              (TrainingChip, MinorPotion, AffinityRune-x4, RareToken).
              Drop table keyed on opponent difficulty. On battle win:
              roll drop table, increment Item.qty for the user. GET
              /api/inventory returns user's items. POST /api/item/apply
              applies an item (e.g. potion → restore HP between battles,
              chip → goes to training UI).
            depends_on:
              - TASK-029
            pr: https://github.com/g-chappell/pirate-battle/pull/51
            completed: '2026-05-11T02:51:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T01:05:00Z'
            attempt_count: 1
          - id: TASK-032
            title: 'Web: inventory UI + apply flow'
            status: done
            priority: med
            complexity: small
            workspaces:
              - apps/web
            description: |
              Inventory page: list items by type with qty. "Use" → modal
              prompting which crew to apply to (if relevant). Update
              optimistically; reconcile on server response.
            depends_on:
              - TASK-031
            pr: https://github.com/g-chappell/pirate-battle/pull/52
            completed: '2026-05-11T04:04:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T03:01:00Z'
            attempt_count: 1
      - id: STORY-13
        title: Battle stats + replay viewer
        tasks:
          - id: TASK-033
            title: 'Server: aggregate per-crew W/L, K/D + GET /api/stats'
            status: done
            priority: med
            complexity: medium
            workspaces:
              - apps/server
            description: |
              Aggregate query reading Battle + BattleEvent: per-user
              total wins, losses, win rate; per-crew K/D, KO contribution,
              winning-team participation. GET /api/stats[?crewId=...].
              Cached for 60s per user.
            depends_on:
              - TASK-029
            pr: https://github.com/g-chappell/pirate-battle/pull/53
            completed: '2026-05-11T05:23:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T05:06:00Z'
            attempt_count: 1
          - id: TASK-034
            title: 'Web: replay viewer (re-render BattleEvents through engine)'
            status: done
            priority: med
            complexity: large
            workspaces:
              - apps/web
              - packages/core
            description: |
              Replay page: GET /api/battle/:id with full BattleEvents.
              Re-step through events in BattleScene with a play/pause/scrub
              control. Same engine code runs locally — replays are
              deterministic by design. Verify via packages/core's
              determinism tests.
            depends_on:
              - TASK-033
              - TASK-019
            pr: https://github.com/g-chappell/pirate-battle/pull/59
            completed: '2026-05-11T11:16:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T11:02:00Z'
            attempt_count: 1
  - id: EPIC-05
    title: Discord bot — fully playable battles
    description: |
      Discord-as-first-class-client. Players link Discord ↔ wallet via a
      one-time signed-message handshake; slash commands run real battles
      against the same backend; embeds render the battle state with HP bars
      + move announcements; messages edit/append as turns resolve.
      Async-friendly so battles can span hours.
    stories:
      - id: STORY-14
        title: Discord bot scaffold + Discord↔wallet identity linking
        tasks:
          - id: TASK-035
            title: Discord bot scaffold + slash command registration tooling
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/discord
            description: |
              apps/discord/src/index.ts: discord.js Client with intents
              [Guilds]. Reads DISCORD_TOKEN. apps/discord/scripts/register-commands.ts:
              REST API call to register commands. Use
              applicationGuildCommands(CLIENT_ID, DEV_GUILD_ID) when
              DISCORD_DEV_GUILD_ID is set (instant dev iteration), else
              applicationCommands (global). Run as a one-off deploy step,
              not on bot startup.
            depends_on:
              - TASK-005
            pr: https://github.com/g-chappell/pirate-battle/pull/44
            completed: '2026-05-10T17:14:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T17:06:00Z'
            attempt_count: 1
          - id: TASK-036
            title: 'Server: /api/discord/link-token + /api/discord/link-claim'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/server
            description: |
              apps/server: POST /api/discord/link-token (auth required by
              wallet session) returns a one-time token (15-min expiry).
              POST /api/discord/link-claim accepts { token, discordUserId }
              from the bot, looks up the User, sets User.discordUserId,
              invalidates token. Reject if token already used / expired.
            depends_on:
              - TASK-023
            pr: https://github.com/g-chappell/pirate-battle/pull/40
            completed: '2026-05-10T13:05:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T13:04:00Z'
            attempt_count: 2
          - id: TASK-037
            title: 'Discord bot: /link command + DM flow → server claim'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/discord
            description: |
              /link slash command: tells the user to visit the web app
              while signed in, run "Generate link token" (web UI
              button → POST /api/discord/link-token), then send the bot
              the token via a follow-up /link-claim <token>. On claim,
              bot calls server's /api/discord/link-claim with the token +
              the invoking Discord user id. Confirmation DM on success.
            depends_on:
              - TASK-035
              - TASK-036
            pr: https://github.com/g-chappell/pirate-battle/pull/45
            completed: '2026-05-10T18:14:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T18:03:00Z'
            attempt_count: 1
      - id: STORY-15
        title: 'Slash commands suite: /team /battle /move /switch /forfeit /stats'
        tasks:
          - id: TASK-038
            title: 'Discord: /team + /battle + /stats slash commands'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/discord
              - apps/server
            description: |
              /team — embed listing user's captains + crews, HP / level /
              moves. /battle <opponent | "ai"> — initiate a Battle row;
              embed-render initial state. /stats [@user] — aggregate W/L
              from /api/stats. Each command resolves Discord user → User
              row via discordUserId; rejects if not linked.
            depends_on:
              - TASK-037
              - TASK-033
            pr: https://github.com/g-chappell/pirate-battle/pull/54
            completed: '2026-05-11T06:26:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T06:04:00Z'
            attempt_count: 1
          - id: TASK-039
            title: 'Discord: /move + /switch + /forfeit (turn actions)'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/discord
              - apps/server
            description: |
              In-battle slash commands: /move <name>, /switch <crewName>,
              /forfeit. Server enforces it's the user's turn. Bot edits
              the original /battle embed (held by message id stored on
              Battle row) with the new state. Out-of-turn → ephemeral
              error. Ephemeral hint includes available actions.
            depends_on:
              - TASK-038
            pr: https://github.com/g-chappell/pirate-battle/pull/55
            completed: '2026-05-11T07:28:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T07:05:00Z'
            attempt_count: 1
      - id: STORY-16
        title: Embed-based battle renderer + async match state machine
        tasks:
          - id: TASK-040
            title: Embed renderer (HP bars, move announcement, type-effectiveness)
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/discord
            description: |
              renderBattleEmbed(state) → EmbedBuilder. HP shown as
              pseudo-graphical bar (`██████░░░░ 60/100`), per-side
              affinity emoji indicator, move log of last 3 turns,
              type-effectiveness call-outs ("super effective!"). Deterministic
              — same state in → same embed out. Pure function; testable.
            depends_on:
              - TASK-038
            pr: https://github.com/g-chappell/pirate-battle/pull/56
            completed: '2026-05-11T08:15:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T08:07:00Z'
            attempt_count: 1
          - id: TASK-041
            title: Async match state machine (Battle messageId, edit lifecycle)
            status: done
            priority: high
            complexity: large
            workspaces:
              - apps/server
              - apps/discord
            description: |
              Battle row tracks discordChannelId + discordMessageId. On
              every action that mutates state, the bot edits the existing
              message via channel.messages.edit. On 15-min interaction-
              token expiry boundary, fall back to channel.send for fresh
              "current state" message; old message is updated to "view
              continued at <link>". State recovery: bot start should
              reconcile in-flight battles (Battle.endedAt is null) by
              re-fetching their messages and resyncing timestamps.
            depends_on:
              - TASK-039
              - TASK-040
            pr: https://github.com/g-chappell/pirate-battle/pull/58
            completed: '2026-05-11T10:38:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T10:03:00Z'
            attempt_count: 1
  - id: EPIC-06
    title: Mobile delivery + Async PvP
    description: |
      Wrap the web build for Android via Capacitor (iOS deferred — no
      macOS access in the autonomous pipeline); sort out mobile wallet
      UX (CIP-45 / WalletConnect). Add async PvP — challenge a friend
      by link, leaderboard, ranked-lite. All against the existing backend
      engine; no new authority anywhere.
    stories:
      - id: STORY-17
        title: Capacitor wrap (Android) + mobile wallet UX
        tasks:
          - id: TASK-042
            title: 'apps/mobile: Capacitor Android scaffold pointing at apps/web build'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/mobile
              - apps/web
            description: |
              apps/mobile: install @capacitor/core + @capacitor/cli +
              @capacitor/android. Add capacitor.config.ts with
              webDir = "../web/dist" and appId/appName set. Run
              `npx cap add android` (results committed). Add scripts:
              build (npm run build -w apps/web && npx cap sync),
              open:android. Add the Android generated-artefact paths
              from CLAUDE.md → Scaffolding hygiene to .gitignore in the
              same PR (android/build/, android/.gradle/,
              android/app/build/). iOS is intentionally out of scope —
              no macOS in the autonomous pipeline; revisit only if/when
              that changes. Verification: `npx cap sync android` exits 0
              and android/ contains a valid Gradle project layout.
            depends_on:
              - TASK-021
            pr: https://github.com/g-chappell/pirate-battle/pull/64
            completed: '2026-05-12T07:36:00Z'
            blocked_reason: null
            last_attempted: '2026-05-12T07:27:00Z'
            attempt_count: 1
          - id: TASK-043
            title: 'Mobile wallet UX: CIP-45/WalletConnect bridge + fallback (Android)'
            status: done
            priority: high
            complexity: large
            workspaces:
              - apps/mobile
              - apps/web
            description: |
              Detect mobile user agent on Android; switch wallet-connect
              path from window.cardano (browser ext) to CIP-45 /
              WalletConnect session. Use @walletconnect/universal-provider
              with the Cardano namespace adapter. For Android wallets
              without CIP-45 support, deep-link to a known dApp browser
              (e.g. Eternl mobile) via Android intent / custom scheme;
              document fallback in docs/MOBILE.md. iOS path is out of
              scope (see EPIC-06 note).
            depends_on:
              - TASK-042
              - TASK-024
            pr: https://github.com/g-chappell/pirate-battle/pull/65
            completed: '2026-05-12T08:04:00Z'
            blocked_reason: null
            last_attempted: '2026-05-12T07:53:00Z'
            attempt_count: 1
      - id: STORY-18
        title: 'Async PvP: challenge-by-link + match queue + persistence'
        tasks:
          - id: TASK-044
            title: 'Server: PvP challenge create/accept + match queue'
            status: done
            priority: high
            complexity: large
            workspaces:
              - apps/server
            description: |
              POST /api/pvp/challenge creates a Challenge row (tokenized
              link, 24h expiry). Anyone with the link who is signed in
              can POST /api/pvp/challenge/:token/accept, which creates a
              PvP-mode Battle. Each user submits Actions independently;
              server resolves a turn once both sides have submitted (or
              an action timeout fires, default 12h). Also: open queue
              mode — POST /api/pvp/queue puts you into a pool, server
              pairs by ELO/random; GET /api/pvp/queue/status polls.
            depends_on:
              - TASK-020
            pr: https://github.com/g-chappell/pirate-battle/pull/41
            completed: '2026-05-10T14:03:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T14:02:00Z'
            attempt_count: 2
          - id: TASK-045
            title: 'Web: PvP UI (generate challenge link, accept, in-progress list)'
            status: done
            priority: high
            complexity: medium
            workspaces:
              - apps/web
            description: |
              PvP page: button "Challenge a friend" → generates link;
              copy-to-clipboard. List of in-progress PvP battles with
              "their move pending" / "your move pending" badges. Click
              opens BattleScene in async mode. "Find a match" → enters
              the queue; pings on match found.
            depends_on:
              - TASK-044
            pr: https://github.com/g-chappell/pirate-battle/pull/46
            completed: '2026-05-10T19:21:00Z'
            blocked_reason: null
            last_attempted: '2026-05-10T19:05:00Z'
            attempt_count: 1
      - id: STORY-19
        title: Leaderboard + season/ladder skeleton
        tasks:
          - id: TASK-046
            title: 'Server: ELO calc + Season + per-season leaderboard endpoint'
            status: done
            priority: med
            complexity: medium
            workspaces:
              - apps/server
              - packages/db
            description: |
              Add Season table (id, name, startsAt, endsAt). Add elo +
              seasonId to user's PvP record. ELO update on PvP battle end
              (K=32). GET /api/leaderboard/:seasonId paginated by ELO desc.
              Cron-equivalent: open a fresh Season on month boundary
              (call out in scripts/season-cron.sh; wire to system cron
              later — out of scope here).
            depends_on:
              - TASK-044
            pr: https://github.com/g-chappell/pirate-battle/pull/60
            completed: '2026-05-11T12:15:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T12:04:00Z'
            attempt_count: 1
          - id: TASK-047
            title: 'Web + Discord: leaderboard views'
            status: done
            priority: med
            complexity: small
            workspaces:
              - apps/web
              - apps/discord
            description: |
              Web: /leaderboard page, paginated. Discord: /leaderboard
              [season] embed, top 10 with rank/name/elo. Both consume
              GET /api/leaderboard/:seasonId.
            depends_on:
              - TASK-046
            pr: https://github.com/g-chappell/pirate-battle/pull/61
            completed: '2026-05-11T13:17:00Z'
            blocked_reason: null
            last_attempted: '2026-05-11T13:04:00Z'
            attempt_count: 1
